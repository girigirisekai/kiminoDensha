package ikuzo.kimi.densha.util;

public class StationCodeToStation {
	
	
	public StationData toCode(String stationCode) {
		StationData sData = new StationData();
		// DB이용하는 코드
		
		// 역코드를 넣으면
		// String자리는 VO가 된다.
		// stationData에 역 데이터(좌표, 역명, 역코드를)가져온다.
		// 가져온 데이터는 다시 돌려준다. 끝

		return sData;
	}
}
